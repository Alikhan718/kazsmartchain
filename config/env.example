NODE_ENV=development

# Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=kaz
POSTGRES_PASSWORD=kazpass
POSTGRES_DB=kazsmartchain

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# API
API_PORT=4000
API_HOST=0.0.0.0
API_BASE_URL=http://localhost:4000
API_JWT_SECRET=dev-secret-change

# OIDC / SAML (dev defaults)
OIDC_ISSUER_URL=http://localhost:8080/realms/dev
OIDC_CLIENT_ID=kaz-console
OIDC_CLIENT_SECRET=change-me
SAML_IDP_METADATA_URL=http://localhost:8081/metadata

# TLS/mTLS
TLS_ENABLED=false
TLS_KEY_PATH=
TLS_CERT_PATH=
MTLS_ENABLED=false
CA_BUNDLE_PATH=

# FireFly / Besu / Tessera
FIREFLY_BASE_URL=http://firefly-mock:5100
FIREFLY_NAMESPACE=default
BESU_PRIVACY_ENABLED=true

# IPFS
IPFS_BASE_URL=http://ipfs:5001

# Solana
SOLANA_RPC_URL=http://solana-mock:8899
SOLANA_CUSTODIAL_KEYPAIR_PATH=./keys/solana-devnet.json
SOLANA_CLUSTER=devnet

# Web
WEB_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_SOLANA_CLUSTER=devnet

# Relay
RELAY_PORT=4100
RELAY_WORKER_CONCURRENCY=4

# Metrics
PROMETHEUS_METRICS_ENABLED=true
PROMETHEUS_METRICS_PORT=9464

